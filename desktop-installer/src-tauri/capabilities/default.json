{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the AetherCloud Setup wizard window. Grants the frontend the ability to invoke our #[tauri::command] handlers AND to listen for backend-emitted events (installer://progress). Without core:event:default, Tauri v2 silently blocks listen() and the UI never receives progress updates even though the backend emits them correctly. That was the 'Backend not responding' bug.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
