# ═══════════════════════════════════════════════════
# AetherCloud-L — Requirements
# Quantum-Secured AI File Intelligence System
# Aether Systems LLC — Patent Pending
# ═══════════════════════════════════════════════════
#
# Floors are set to the lowest version KNOWN FREE of published CVEs as of
# the 2026-04 security sweep. Do not weaken these without re-running
# `pip-audit` / npm-audit-style CVE checks. Ideally generate a full
# requirements.lock from this file for reproducible deploys.

# ─── Protocol-L Dependencies ─────────────────────
qiskit>=2.0.0
qiskit-ibm-runtime>=0.46.1
qiskit-aer>=0.17.2
pyasn1>=0.5.0

# ─── AetherCloud-L Dependencies ──────────────────
bcrypt>=4.0.0
watchdog>=3.0.0
rich>=13.0.0
anthropic>=0.97.0
textual>=0.50.0
# cryptography: CVE-2023-50782 (Bleichenbacher) fixed in 42.0.0,
# CVE-2024-26130 (NULL-deref) fixed in 42.0.4. Floor bumped to 44 per
# audit M8.
cryptography>=44.0.0
python-dotenv>=1.0.0
# requests: CVE-2024-47081 (.netrc cred leak) fixed in 2.32.4.
requests>=2.32.4

# ─── DO Spaces (vault cloud storage) ──────────────
boto3>=1.42.95

# ─── FastAPI Server (Electron bridge) ────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
# python-multipart: CVE-2024-53981 (multipart DoS) fixed in 0.0.18. Audit M7.
python-multipart>=0.0.18
slowapi>=0.1.9

# ─── Supabase (license validation backend) ──────
# Python client for querying public.users in the AetherCloud Supabase
# project. Floor pinned to 2.9; upper bound <3.0 locks out the next
# major-version bump until we re-test. Added 2026-04-18 (Sequence 1:
# license validation route). Transitive deps include httpx, gotrue,
# postgrest, storage3, realtime — see PR description for dry-run output.
supabase>=2.9,<3.0

# ─── Development / Testing ───────────────────────
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.27.0
