node_modules/
dist/
.env
.env.local
.env.production
.DS_Store
*.log

# The zip + raw PNGs that lived next to the webp sources; we don't redistribute them
aethersecurity-website.zip
*.png
!src/assets/**/*.png
!public/**/*.png

# Build artifact — regenerated by `npm run build` + Compress-Archive.
# See CLOUDFLARE-DEPLOY.md. Don't commit; a fresh build is always preferable.
aethersystems-net-cloudflare-pages.zip
